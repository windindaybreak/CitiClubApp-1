<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.citiclubapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".successfulRegis.SuccessfulRiges"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            ></activity>
        <activity
            android:name=".real_name_authentificationActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" /> <!-- 注册界面 -->
        <activity
            android:name=".RigesterActivity.RigesterActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" /> <!-- 登录界面 -->
        <activity
            android:name=".LoginActivity.LoginActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" /> <!-- 启动界面（主界面） -->
        <activity
            android:name=".SplashPage.splashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>